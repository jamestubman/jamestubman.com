import styled from "styled-components"
import Container from "react-bootstrap"
export const Navigation = styled.div(() => ({
    color: "#ffffff",
    width: "100%",
    backgroundColor: "#353b48",
}))

export const NavItem = styled(Container)(() => ({
    wi,
}))
